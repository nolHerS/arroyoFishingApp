# Configuración de H2 Database para tests
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA/Hibernate - CONFIGURACIÓN CRÍTICA
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.hbm2ddl.auto=create-drop

# Asegurar que Hibernate escanee las entidades
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Desactivar inicialización con scripts SQL
spring.sql.init.mode=never

# Otras configuraciones
spring.main.allow-bean-definition-overriding=true
spring.h2.console.enabled=true

# JWT (valores de prueba)
jwt.secret=testsecretfortestingtestsecretfortesting
jwt.expiration=3600
jwt.refresh-token.expiration=86400

# Cloudinary (valores de prueba)
cloudinary.cloud-name=dzmoynpmm
cloudinary.api-key=959522869789247
cloudinary.api-secret=V4xPpPn_NvHAqafhFqbECxvO8TI

# Configuración de imágenes
app.image.max-size=10485760
app.image.allowed-types=image/jpeg,image/png,image/webp
app.image.thumbnail.width=300
app.image.thumbnail.height=300
app.image.max-images-per-capture=5